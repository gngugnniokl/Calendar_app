<?php
/**
 * Leaderboard - Main View Layout
 */

// Check the URL for the 'type=full' parameter
$isFullView = (isset($_GET['type']) && $_GET['type'] === 'full');
?>
<div class="leaderboard-page">
    <!-- Top 7 View -->
    <div id="leaderboard-view-top7" class="leaderboard-view <?php echo $isFullView ? '' : 'is-active'; ?>" <?php echo $isFullView ? 'hidden' : ''; ?>>
        <?php include __DIR__ . '/top7.phtml'; ?>
    </div>

    <!-- Full Rankings View -->
    <div id="leaderboard-view-full" class="leaderboard-view <?php echo $isFullView ? 'is-active' : ''; ?>" <?php echo $isFullView ? '' : 'hidden'; ?>>
        <?php include __DIR__ . '/full_rankings.phtml'; ?>
    </div>
</div>